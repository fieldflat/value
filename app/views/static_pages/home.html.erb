<% provide(:title, "Home") %>

<!-- 検索フォーム -->
<%= render "layouts/search" %>

<% if (!logged_in? && !(defined? @search_words)) %>
  <!-- スライドショー -->
  <%= render "layouts/slideshow" %>
<% end %>

<!-- 最近の商品情報 -->
<%= render "layouts/recent_items" %>

<!-- おすすめの商品情報 -->
<% if (!logged_in? && !(defined? @search_words)) %>
  <%= render "layouts/recommend_items" %>
<% end %>

<% if logged_in? %>

<% else %>
  <!-- Valueの紹介 -->
  <%= render "layouts/introduction" %>

  <!-- Valueで、繋がろう。 -->
  <%= render "layouts/register_login" %>
<% end %>
